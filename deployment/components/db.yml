---
  - include: "{{root_dir}}/tasks/postgres_9.4.yml"
    tags: db

  - name: Database | Create db
    postgresql_db: name={{db_user}}
    sudo_user: postgres
    sudo: yes
    tags: db

  - name: Database | Create user
    postgresql_user: name={{db_name}} password={{db_password}} role_attr_flags=SUPERUSER
    sudo_user: postgres
    sudo: yes
    tags: db


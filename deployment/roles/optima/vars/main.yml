user: "optima"
port: "{{ app.port }}"
giturl: "{{ app.giturl }}"

dir: "/home/{{ user }}/installations/{{ app.name }}"
daemon: "/software/anaconda/bin/python {{ dir }}/bin/run_server.py"
daemon_opts: "{{ port }}"
service_name: "{{ app.name }}"
branch: "{{ app.branch }}"
repo: "{{ app.repo }}"

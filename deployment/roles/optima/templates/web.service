[Unit]
Description=Optima {{ service_name }}
Wants={{ service_name }}celery.service

[Service]
ExecStart={{ daemon }} {{daemon_opts}}
Restart=always
User={{ user }}
WorkingDirectory={{ dir }}

[Install]
WantedBy=multi-user.target
